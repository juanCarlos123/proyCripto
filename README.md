#Página con información sobre la criptografía.

Instalación y configuración.
  - Coloca est carpeta en la carpeta raiz del servidor web (htdocs).
  - Accede a la ruta de la carpeta en el servidor web.

